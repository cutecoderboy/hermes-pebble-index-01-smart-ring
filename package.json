{
  "name": "hermes-pebble-index-01-smart-ring",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker bridging the Pebble Index 01 ring's webhook to a self-hosted Hermes Agent",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "check": "tsc --noEmit",
    "mock-hermes": "node scripts/mock-hermes.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.20260823.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.125.0"
  }
}
